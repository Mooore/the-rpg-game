<UserControl x:Class="RPG_game_GUI.Menu.OptionsContent.Video"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="0.5*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Button     x:Name="btn1920" 
                    Content="1920x1024" 
                    Grid.Row="1" 
                    Grid.Column="1" 
                    HorizontalAlignment="Stretch" 
                    VerticalAlignment="Stretch" 
                    Margin="10,10,10,10" 
                    Width="auto" 
                    MaxWidth="200" 
                    MinWidth="100"
                    Height="auto"
                    MaxHeight="50"
                    MinHeight="30"
                    Click="btn1920_Click"/>

        <Button     x:Name="btn1366" 
                    Content="1366x768" 
                    Grid.Row="2" 
                    Grid.Column="1" 
                    HorizontalAlignment="Stretch" 
                    VerticalAlignment="Stretch" 
                    Margin="10,10,10,10" 
                    Width="auto" 
                    MaxWidth="200" 
                    MinWidth="100"
                    Height="auto"
                    MaxHeight="50"
                    MinHeight="30"
                    Click="btn1366_Click"/>

        <Button     x:Name="btn1024" 
                    Content="1024x768" 
                    Grid.Row="3" 
                    Grid.Column="1" 
                    HorizontalAlignment="Stretch" 
                    VerticalAlignment="Stretch" 
                    Margin="10,10,10,10" 
                    Width="auto" 
                    MaxWidth="200" 
                    MinWidth="100"
                    Height="auto"
                    MaxHeight="50"
                    MinHeight="30"
                    Click="btn1024_Click"/>

        <Button x:Name="btnBack" Content="Back" Grid.Row="43" Grid.Column="2" HorizontalAlignment="Right" Margin="10,10,10,10" VerticalAlignment="Top" Width="75" Click="btnBack_Click"/>
    </Grid>
</UserControl>
